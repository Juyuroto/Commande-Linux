services:
  linux-command:
    image: docker-command:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: linux-command
    ports:
      - "3006:80"
    restart: "no"
    environment:
      - NODE_ENV=production
    networks:
      - guide-network

networks:
  guide-network:
    driver: bridge
